{# source: https://stackoverflow.com/a/65082524/1814910 #}
<ul>
    {% for inventory in field.value %}
        {%- set url = ea_url()
        .setController('App\\Controller\\Admin\\InventoryCrudController')
        .setAction('detail')
        .setEntityId(inventory.id)
        -%}
        <li>
            <a href="{{ url }}">
            {{ inventory }}
            </a>
        </li>
        {% else %}
        </ul>  
        <span class="badge badge-secondary">None</span>
        <ul>
    {% endfor %}
</ul>
