{# source: https://stackoverflow.com/a/65082524/1814910 #}
<ul>
    {% for member in field.value %}
        {%- set url = ea_url()
        .setController('App\\Controller\\Admin\\MemberCrudController')
        .setAction('detail')
        .setEntityId(member.id)
        -%}
        <li>
            <a href="{{ url }}">
            {{ member.name }}
            </a>
        </li>
        {% else %}
        </ul>  
        <span class="badge badge-secondary">None</span>
        <ul>
    {% endfor %}
</ul>
