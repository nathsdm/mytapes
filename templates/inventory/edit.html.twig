{% extends 'base.html.twig' %}

{% block title %}Edit Inventory{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center">Edit Inventory</h1>

        <div class="row justify-content-center">
            <div class="col-md-6">
                {{ include('inventory/_form.html.twig', {'button_label': 'Update'}) }}

                <a href="#" onclick="history.back()" class="btn btn-secondary mt-3">Back</a>
            </div>
        </div>
    </div>
{% endblock %}
